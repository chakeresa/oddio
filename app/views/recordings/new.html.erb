<%= form_for @recording, url: landmark_recordings_path(@recording.landmark) do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <br>
  <%= f.file_field :recording, accept:"audio/*", capture: true %><br>
  <%= f.submit %>
<% end %>
